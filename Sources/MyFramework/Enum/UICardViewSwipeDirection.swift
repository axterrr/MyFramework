import UIKit

/// Represents the direction in which a card was swiped.
public enum UICardViewSwipeDirection {
    /// The user swiped the card to the left.
    case left
    /// The user swiped the card to the right.
    case right
}
